// src/domain/value-objects/Email.ts
// Value Object para encapsular validación simple de correo electrónico.
//export class Email {
  //constructor(public readonly value: string) {}

  //isValid(): boolean {
    //return this.value.includes('@') && this.value.trim().length > 5
  //}
//}

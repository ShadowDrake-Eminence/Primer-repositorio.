// src/presentation/middlewares/ValidateUserPayload.ts
// Middleware simple para validar la entrada antes de pasar al controlador.
// export function validateUserPayload(payload: any): void {
//   if (!payload || typeof payload.name !== 'string' || typeof payload.email !== 'string') {
//     throw new Error('Payload inválido: se requiere name y email')
//   }
// }
